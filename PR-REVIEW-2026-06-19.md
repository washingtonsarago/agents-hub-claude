# Revisão de Pull Requests — agents-hub-claude

**Data:** 2026-06-19 · **Repositório:** `EMS-NCTECH/agents-hub-claude` · **Execução:** tarefa agendada `verificao` (autônoma)

---

## ⚠️ Limitação do ambiente (leia primeiro)

A consulta **ao vivo** dos Pull Requests no GitHub continua indisponível:

- `api.github.com` segue **bloqueado** pela allowlist de egress (`curl` → `HTTP 000`).
- A sandbox **não tem credenciais**: `git fetch` manual falha com `could not read Username for 'https://github.com'`; `gh` CLI não está instalado.

**Novidade desta execução:** os refs do clone local **deixaram de estar congelados em 2026-05-07**. Houve um *fetch* bem-sucedido (provavelmente fora da sandbox): `origin/main` foi atualizado em **2026-06-19 00:48** e agora aponta para `ecd9187 Merge pull request #1 from .../feature/ux-mobile-web-agents` (2026-06-18 18:51). Ou seja, há dados novos a relatar, ainda que eu não consiga listar o conjunto vivo de PRs abertos no GitHub neste exato momento.

**Para tornar a automação 100% confiável:** liberar `api.github.com` na egress **e** prover um token (PAT/`gh` CLI) à sandbox. Sem isso, dependo de quando o clone local é atualizado por fora.

---

## Resumo executivo

**Nenhum PR aberto novo exige ação hoje.** O único evento novo desde o último run (2026-06-18 12:19) foi a **mesclagem do PR #1**, que já estava aberto e agora está **fechado/merged** na `main`. As demais branches candidatas permanecem **inalteradas** e **stale** — mesmas conclusões dos runs anteriores.

| Branch (candidato a PR) | Commit | Estado | Faz sentido | Atende ao padrão | Recomendação |
|---|---|---|:---:|:---:|---|
| `feature/ux-mobile-web-agents` | `8491d32` → `ecd9187` | ✅ **Merged (PR #1)** | **9/10** | **10/10** | ✅ Aprovado (já mesclado) |
| `feature/add-cache-search-engineer` | `2fa94b8` | stale (1↑ / 14↓) | 3/10 | 4/10 | ❌ Rejeitar / fechar |
| `feature/add-infra-cost-estimator` | `b5062e5` | stale (1↑ / 14↓) | 3/10 | 4/10 | ❌ Rejeitar / fechar |
| `feature/add-technical-writer` | `76fa323` | stale (1↑ / 14↓) | 3/10 | 4/10 | ❌ Rejeitar / fechar |

> `feature/add-senior-product-designer` está a **0 commits à frente** da `main` (já mesclada) — nada a revisar.
> `fix/regen-no-spurious-date-bump` é branch **apenas local** (não existe em `origin`) — não é PR aberto, excluída.

---

## 1. `feature/ux-mobile-web-agents` — **PR #1 (mesclado)** 🟢

**Antes:** `8491d32 feat(agents): add ux-designer-mobile and ux-designer-web` · 2026-06-18 18:49
**Merge:** `ecd9187 Merge pull request #1` · 2026-06-18 18:51 · diff de mesclagem: **+403 / −1 em 3 arquivos**

Este é o trabalho **novo** desta janela. Apesar de já estar mesclado, vale o parecer retrospectivo porque define a barra de qualidade do repositório.

**Conteúdo.** Dois agents — `ux-designer-mobile` e `ux-designer-web` — mais o registro correspondente em `manifest.json`. Escopo cirúrgico: **só** os arquivos que deveriam mudar, nada de ruído.

**Aderência ao padrão (exemplar):**

- **Frontmatter completo e correto** nos dois: `name`, `description` (rica, com exemplos de acionamento em PT-BR e distinção entre os dois agents — mobile vs. web), `model: opus`, `color: pink`, `tier: reasoning` e — decisivo — o campo obrigatório **`team: product`** presente. É exatamente o que faltava nas branches stale.
- **`manifest.json` atualizado corretamente:** novas entradas com `name`, `version: 1.0.0`, `path`, `sha256`, `scope: user`, `description` e `team: product`; `updated_at` promovido para `2026-06-18`.
- **Corpo bem estruturado** (185 e 199 linhas), seguindo o gabarito da casa: `Mission`, `Memory discipline`, `Core principles`, `Domain`, `Frameworks`, `Workflow`, `Output formats`, `Standards` (com subseções de Research, IA/navegação, Interação, Sistema visual/plataforma, Acessibilidade, Performance, Medição), `Collaboration protocol`, `Self-verification checklist` e `Anti-patterns`.
- **Domínio bem fundamentado:** mobile cita Apple HIG e Material Design 3, thumb-reach, estados offline/sync, a11y para VoiceOver/TalkBack; web cita WCAG 2.1 AA, ARIA, heurísticas de Nielsen, breakpoints e performance-budget. Boa separação de responsabilidades entre os dois agents.
- **Convenções de commit:** título do feature commit segue Conventional Commits (`feat(agents): ...`); merge via PR.

**Notas (escala 1–10):**

- **Faz sentido: 9/10** — dois agents coerentes, necessários e sem sobreposição; cobre uma lacuna real (UX mobile/web) com escopo limpo.
- **Atende ao padrão: 10/10** — cumpre todas as convenções do repo, inclusive o `team:` obrigatório e o registro no manifesto.

**Pontos fortes.** Escopo mínimo e correto; frontmatter e manifesto impecáveis; `description` orientada a acionamento; estrutura de corpo consistente com o restante do hub.

**Pontos fracos / melhorias (menores, pós-merge).** Ambos saíram como `version: 1.0.0` — ok para estreia. Eventual sobreposição de fronteira com um futuro `senior-product-designer` (já no hub) merece uma linha de "quando NÃO me use" apontando para ele, para reduzir ambiguidade de roteamento. Nada disso é bloqueante.

**Recomendação:** ✅ **Aprovado.** Já mesclado e em estado superior; serve de referência de qualidade para os demais PRs.

---

## 2. `feature/add-cache-search-engineer` (stale) ❌

**Commit:** `2fa94b8 feat(agents): add cache-search-engineer` · 2026-05-07 · **1↑ / 14↓** vs `main`.

Inalterada desde os runs anteriores. O agent já está na `main` em versão superior **com `team: data`**; a branch **não tem `team:`** e está baseada num `main` antigo, de modo que mesclá-la **regrediria** o repositório (remove o `team:` e reverte dezenas de arquivos).

- **Faz sentido: 3/10** · **Atende ao padrão: 4/10**
- **Melhoria:** se houver delta a recuperar, abrir PR **rebaseado na `main` atual** preservando `team: data`.
- **Recomendação:** ❌ **Rejeitar / fechar.**

## 3. `feature/add-infra-cost-estimator` (stale) ❌

**Commit:** `b5062e5` · 2026-05-07 · **1↑ / 14↓**. Já na `main` com `team: devops`; branch sem `team:` e defasada.

- **Faz sentido: 3/10** · **Atende ao padrão: 4/10**
- **Melhoria:** novo PR rebaseado preservando `team: devops`.
- **Recomendação:** ❌ **Rejeitar / fechar.**

## 4. `feature/add-technical-writer` (stale) ❌

**Commit:** `76fa323` · 2026-05-07 · **1↑ / 14↓**. Já na `main` com `team: docs`; branch sem `team:` e defasada.

- **Faz sentido: 3/10** · **Atende ao padrão: 4/10**
- **Melhoria:** novo PR rebaseado preservando `team: docs`.
- **Recomendação:** ❌ **Rejeitar / fechar.**

---

## Parecer final consolidado

O run de hoje tem uma diferença real frente aos anteriores: o **PR #1 (`ux-mobile-web-agents`) foi mesclado** e é **trabalho de alta qualidade** — escopo cirúrgico, frontmatter e `manifest.json` corretos, e o campo obrigatório **`team: product`** presente. É o contraste exato com as branches stale, cujo defeito central sempre foi a ausência de `team:` somada à base defasada.

Sobre os PRs **ainda abertos**: as três branches `feature/add-*` permanecem **stale e inalteradas** — mesclá-las reverteria a `main`. A ação correta segue sendo **fechá-las** sem merge e, se houver algo a aproveitar, reabrir PR rebaseado mantendo o `team:` correto. **Não há, no snapshot local, nenhum PR aberto novo exigindo revisão.**

**Ressalva metodológica:** este parecer reflete os refs do clone local (atualizados pela última vez em 2026-06-19 00:48). Sem acesso a `api.github.com` + credenciais, não consigo confirmar se existem PRs abertos no GitHub criados após esse ponto. Para verificação ao vivo confiável, é necessário desbloquear a API e prover um token à sandbox.
