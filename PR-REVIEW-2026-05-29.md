# Relatório de Análise de Pull Requests

**Repositório:** `EMS-NCTECH/agents-hub-claude`
**Data da execução:** 2026-05-29
**Executado por:** Tarefa agendada `verificao` (análise automática de PRs)

---

## Limitação importante deste run

O ambiente sandbox **continua sem acesso ao GitHub**. Refs remotos locais permanecem **congelados em 2026-05-07** — agora **22 dias** sem fetch real:

- Não há `gh` CLI nem `GITHUB_TOKEN`/`GH_TOKEN`. `git fetch origin` falha com `fatal: could not read Username for 'https://github.com': No such device or address`.
- `api.github.com` segue fora da allowlist de egress; o repo é private/internal, então `github.com/.../pulls` retorna 404 sem credenciais.
- Qualquer PR criado, atualizado, mergeado ou fechado depois de 2026-05-07 **não é visível** por esta task.

> **Para que esta verificação reflita o estado real do GitHub,** é preciso (a) liberar `api.github.com` em Settings → Capabilities e (b) configurar credenciais GitHub na sandbox (`gh auth login` ou credential helper). Enquanto isso, os relatórios seguem espelhando o snapshot de 2026-05-07.

---

## Sumário executivo

Comparação com o run anterior (`PR-REVIEW-2026-05-20.md`):

- **Zero alterações** nos refs locais — HEAD SHAs das 4 branches candidatas e de `origin/main` são bit-a-bit idênticos.
- As **mesmas 4 branches candidatas a PR aberto** continuam presentes (autor: Washington Sarago), sem mudança desde 2026-05-07.
- `origin/main` segue em `220b248` (Merge PR #9, 2026-05-07 16:27 -03).

| # | Branch | HEAD | vs `main` | Faz sentido | Atende ao padrão | Recomendação |
|---|---|---|---|---|---|---|
| 1 | `feature/add-cache-search-engineer` | `2fa94b8` | +1 / −10 | 3/10 | 7/10 | Rejeitar / fechar (superado) |
| 2 | `feature/add-infra-cost-estimator` | `b5062e5` | +1 / −10 | 3/10 | 7/10 | Rejeitar / fechar (superado) |
| 3 | `feature/add-technical-writer` | `76fa323` | +1 / −10 | 3/10 | 7/10 | Rejeitar / fechar (superado) |
| 4 | `fix/regen-no-spurious-date-bump` | `d047058` | +1 / −11 | 4/10 | 8/10 | Rejeitar / fechar (superado) |

**Recomendação geral:** as 4 branches estão **superadas pelo conteúdo já presente em `main`**. Em todos os casos `main` contém uma versão igual ou evoluída. Recomenda-se **fechar como obsoleto e deletar a branch** — não há merge útil a fazer.

---

## Verificação técnica deste run (confirmada contra o snapshot local)

A análise não é só herdada: re-validei o diff de cada branch contra `origin/main` hoje.

**PRs 1–3 (novos agents).** Os três arquivos `agents/*.md` **já existem em `main`** e diferem da versão da branch por **exatamente 3 linhas**: a adição do campo `team:` no frontmatter (`team: data`, `team: devops`, `team: docs`). Ou seja, o conteúdo do agent já foi incorporado ao `main` (via PRs posteriores, parte do trabalho de *team-grouping* mergeado em #9), e **a versão da branch é pior** — não tem o campo `team`, que virou convenção obrigatória (todos os 18 agents em `main` o possuem). Mergear qualquer uma destas branches **regrediria** o frontmatter.

- Pontos fortes: prompts bem construídos, descrições com exemplos em PT, posição correta no `manifest.json`, delimitação clara de escopo vs. agents vizinhos.
- Pontos fracos: omitem `team:`; `version: 1.0.0` desatualizado; branches 10 commits atrás de `main`; conteúdo já mergeado.

**PR 4 (fix do regen).** A correção (`updated_at` preservado salvo mudança real de conteúdo) é **tecnicamente correta e cirúrgica**, mas **já está em `main`** — `scripts/regen-manifest.js` no `main` contém as linhas `updated_at: current.updated_at` (l. 252) e `next.updated_at = todayUTC()` (l. 297), além de evoluções posteriores (propagação *side-channel* do campo `team` para o manifest sem exigir bump de versão). Mergear a branch hoje **reverteria** essas evoluções.

- Pontos fortes: patch enxuto, comentário de header atualizado, mensagem em Conventional Commits, área já coberta por `test/regen.test.js`.
- Pontos fracos: chegou tarde — o fix e mais melhorias já estão no `main`.

> As notas de **Faz sentido** são baixas pelo *timing*, não pela qualidade intrínseca (que é boa, 8–9 no momento da criação). Hoje nenhuma das mudanças acrescenta valor sobre `main`.

A análise detalhada por PR (seções completas de pontos fortes/fracos e parecer) permanece válida em [`PR-REVIEW-2026-05-13.md`](./PR-REVIEW-2026-05-13.md). **Não há fato novo a registrar em 2026-05-29** além da confirmação de que o estado local segue inalterado.

---

## Ação sugerida ao responsável

1. **No GitHub real**, confirmar se estas 4 branches/PRs ainda estão abertas. Se sim, fechá-las como *superseded* e deletar as branches.
2. **Destravar a automação**: liberar `api.github.com` + credenciais na sandbox, para que os próximos runs avaliem PRs de fato novos em vez do snapshot congelado.
