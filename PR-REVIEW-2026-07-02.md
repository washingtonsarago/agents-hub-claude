# Verificação de Pull Requests — 2026-07-02

> **Limitação do ambiente (leia primeiro).** Esta execução roda numa sandbox sem
> acesso à API do GitHub. `api.github.com` continua **bloqueado** pela allowlist de
> egress (`curl` → HTTP `000`) e o repo `EMS-NCTECH/agents-hub-claude` é privado, sem
> credenciais nem `gh` CLI disponíveis. Portanto **não consigo consultar a lista real
> de PRs abertos no GitHub** — a análise abaixo é feita sobre os *refs remotos* do
> clone local (`FETCH_HEAD` renovado hoje, 2026-07-02 00:37 UTC, por um `git fetch`
> executado fora da sandbox).
>
> **Próximo passo para tornar a automação plena:** liberar `api.github.com` em
> Settings → Capabilities **e** configurar um token de leitura (`gh auth` / PAT) para
> a sandbox conseguir chamar `/pulls`.

## Estado dos refs (comparação com run anterior)

| Ref | Commit | Data | ↑ / ↓ vs `main` | Mudança desde 06-26/07-01 |
|-----|--------|------|-----------------|----------------------------|
| `origin/main` | `ecd9187` | 2026-06-18 | — | Sem mudança |
| `feature/ux-mobile-web-agents` | `26af678` | 2026-06-23 | 1 ↑ / 1 ↓ | Sem mudança |
| `feature/add-cache-search-engineer` | `2fa94b8` | 2026-05-07 | 1 ↑ / 14 ↓ | Sem mudança (stale) |
| `feature/add-infra-cost-estimator` | `b5062e5` | 2026-05-07 | 1 ↑ / 14 ↓ | Sem mudança (stale) |
| `feature/add-technical-writer` | `76fa323` | 2026-05-07 | 1 ↑ / 14 ↓ | Sem mudança (stale) |
| `feature/add-senior-product-designer` | `e59f133` | 2026-05-05 | 0 ↑ / 16 ↓ | Já incorporada — pode fechar |

**Resumo:** nenhum PR novo aberto. Os refs estão idênticos aos das runs de 06-26 e
07-01. O único ref à frente de `main` com trabalho real é `feature/ux-mobile-web-agents`.

---

## PR candidato #1 — `feature/ux-mobile-web-agents` (`26af678`)

**Commit:** `feat(skills): add adr, product-brief, pr-review, test-plan, postmortem`

**Conteúdo (12 arquivos, +790 / −1):** cinco novas skills (`adr`, `product-brief`,
`pr-review`, `test-plan`, `postmortem`), cada uma com `SKILL.md` + template, além de
atualização de `manifest.json` e do backlog em `docs/discovery/skills-backlog.md`.

### Verificações executadas localmente
- **Conventional Commits:** ✅ `feat(skills): ...`
- **`regen-manifest --check`:** ✅ *manifest is up to date — no changes*
- **Testes Node (`node --test`):** ✅ **43/43 passam, 0 falhas**
- **Estrutura de skills:** ✅ idêntica ao padrão das skills existentes (`SKILL.md` + `templates/`)

### Notas
- **Faz sentido:** **9/10** — skills coerentes e úteis; a skill `pr-review` inclusive
  formaliza este próprio fluxo de revisão. Mudança pequena, focada e bem estruturada.
- **Atende ao padrão:** **9/10** — segue convenções, manifest consistente, testes verdes.
  Único ponto: está 1 commit atrás de `main` (rebase trivial necessário).

### Parecer final
**Pontos fortes:** escopo enxuto e coeso, documentação por template, manifest e testes
consistentes, nenhum débito introduzido. **Pontos fracos:** precisa de rebase sobre
`main` (1 ↓). **Recomendação: APROVAR** após rebase trivial.

---

## Branches stale de agents (candidatos a PR antigos)

`feature/add-cache-search-engineer`, `feature/add-infra-cost-estimator` e
`feature/add-technical-writer` — cada uma 1 ↑ / 14 ↓ vs `main`, congeladas em
2026-05-07. Os agents em si são bem escritos, **mas a frontmatter só tem `tier:` e
segue sem o campo obrigatório `team:`** (convenção introduzida após a criação dessas
branches; `main` já exige `team: data/devops/docs`). Confirmado nesta run:

```
cache-search-engineer  → name, model, tier   (sem team:)
infra-cost-estimator   → name, model, tier   (sem team:)
technical-writer       → name, model, tier   (sem team:)
```

- **Faz sentido:** 6/10 (conteúdo ok, mas desatualizado e muito atrás de `main`)
- **Atende ao padrão:** 4/10 (falta `team:`; manifest desatualizado; 14 commits atrás)
- **Recomendação:** **solicitar mudanças** (rebase + adicionar `team:` + regenerar
  manifest) ou, se abandonadas, **fechar/rejeitar**.

`feature/add-senior-product-designer` está 0 ↑ vs `main` (já incorporada) →
**fechar**.

---

## Conclusão da execução

Nenhum PR novo desde a última run. `feature/ux-mobile-web-agents` permanece o único
candidato com trabalho real e continua **APROVÁVEL** (43/43 testes verdes, manifest
ok) após rebase trivial. As três branches stale seguem sem `team:` →
**solicitar mudanças ou fechar**. Para a automação deixar de produzir relatórios
espelhados, é necessário liberar `api.github.com` + auth na sandbox.
