# Revisão de Pull Requests — agents-hub-claude

**Data:** 2026-06-13 · **Repositório:** `EMS-NCTECH/agents-hub-claude` · **Execução:** tarefa agendada `verificao` (autônoma)

---

## ⚠️ Limitação do ambiente (leia primeiro)

Esta análise **não conseguiu consultar o estado ao vivo dos Pull Requests no GitHub**:

- `api.github.com` continua **bloqueado** pela allowlist de egress.
- O repositório é **privado** e a sandbox **não tem credenciais** — `git fetch` falha com `could not read Username for 'https://github.com'`. `gh` CLI não está instalado.
- Portanto, a análise se baseia apenas nos **refs remotos do clone local**, congelados no último *fetch* bem-sucedido (`origin/main` = merge do PR #9, **2026-05-07**).

**Consequência:** PRs criados *depois* desse fetch são invisíveis aqui. O que segue avalia as únicas branches que aparecem "à frente da main" no clone local. O resultado é, por construção, idêntico aos runs anteriores (espelhado) enquanto o ambiente permanecer congelado.

**Próximo passo para tornar a automação confiável:** liberar `api.github.com` na egress **e** configurar um token (PAT/`gh` CLI) na sandbox. Sem isso, os runs diários produzem relatórios espelhados sobre dados estáticos.

---

## Resumo executivo

Nenhuma das 4 branches candidatas representa trabalho novo a ser integrado. **Todas já estão refletidas na `main`** — e, nos três agents, a versão na `main` está **melhor** que a da branch (o campo obrigatório `team:` foi adicionado durante o merge). **Mesclar qualquer uma delas como está causaria regressão** (remoção do `team:`). No caso do fix, a lógica já está presente na `main`.

| Branch (candidato a PR) | Faz sentido | Atende ao padrão | Recomendação |
|---|:---:|:---:|---|
| `feature/add-cache-search-engineer` | 3/10 | 4/10 | ❌ Rejeitar / fechar (já na main, sem `team:`) |
| `feature/add-infra-cost-estimator` | 3/10 | 4/10 | ❌ Rejeitar / fechar (já na main, sem `team:`) |
| `feature/add-technical-writer` | 3/10 | 4/10 | ❌ Rejeitar / fechar (já na main, sem `team:`) |
| `fix/regen-no-spurious-date-bump` | 4/10 | 7/10 | ❌ Fechar (fix já presente na main) |

> As demais branches remotas (`chore/readme-roadmap-drift`, `docs/usage-team-grouping-and-new-agents`, `feature/add-cost-search-writer-agents`, `feature/add-senior-product-designer`, `feature/ahc-doctor`) estão a **0 commits à frente** da `main` — já mescladas, nada a revisar.

---

## 1. `feature/add-cache-search-engineer`
**Commit:** `2fa94b8 feat(agents): add cache-search-engineer` · 2026-05-07
**Arquivos:** `agents/cache-search-engineer.md` (+325), `manifest.json`

**Conteúdo.** O agent é bem escrito: missão clara, descrição rica com exemplos de acionamento, distinção explícita de `postgres-dba`/`system-architect`. Frontmatter com `name`, `description`, `model: opus`, `color`, `tier: reasoning`.

**Problema decisivo.** O arquivo **já existe na `main`** e a versão da `main` inclui `team: data` na frontmatter — convenção obrigatória do repositório. A branch **não tem** o campo `team:`. Mesclá-la sobrescreveria a versão correta com uma desatualizada.

- **Faz sentido: 3/10** — o agent é útil, mas a mudança já foi integrada; não há trabalho novo.
- **Atende ao padrão: 4/10** — viola a convenção `team:` que a própria `main` já satisfaz.

**Recomendação:** ❌ **Rejeitar / fechar.** Conteúdo já na `main` em estado superior.

---

## 2. `feature/add-infra-cost-estimator`
**Commit:** `b5062e5 feat(agents): add infra-cost-estimator` · 2026-05-07
**Arquivos:** `agents/infra-cost-estimator.md` (+262), `manifest.json`

**Conteúdo.** Agent coerente para estimativa de custo de infraestrutura. Boa frontmatter e descrição.

**Problema decisivo.** Idêntico ao caso anterior: arquivo **já na `main`** com `team: devops`; a branch não traz o campo. Merge = regressão.

- **Faz sentido: 3/10**
- **Atende ao padrão: 4/10**

**Recomendação:** ❌ **Rejeitar / fechar.**

---

## 3. `feature/add-technical-writer`
**Commit:** `76fa323 feat(agents): add technical-writer` · 2026-05-07
**Arquivos:** `agents/technical-writer.md` (+238), `manifest.json`

**Conteúdo.** Agent de redação técnica bem estruturado.

**Problema decisivo.** **Já na `main`** com `team: docs`; branch sem o campo. Merge = regressão.

- **Faz sentido: 3/10**
- **Atende ao padrão: 4/10**

**Recomendação:** ❌ **Rejeitar / fechar.**

---

## 4. `fix/regen-no-spurious-date-bump`
**Commit:** `d047058 fix(regen): only bump updated_at when content actually changed` · 2026-05-06
**Arquivo:** `scripts/regen-manifest.js` (+5 / −6)

**Conteúdo.** Boa correção: evita que `regen-manifest.js` bump o `updated_at` do manifesto todo dia mesmo sem mudança real — só atualiza quando há `totalChanges > 0`. Bem escrita e segue o estilo do arquivo (Conventional Commits, comentário do cabeçalho atualizado).

**Problema decisivo.** A lógica e o comentário **já estão presentes na `main`** (`scripts/regen-manifest.js` da main já contém "Sets updated_at to today's ISO date (UTC) only when content actually changed"). O fix foi aplicado por outro caminho. Tecnicamente é o melhor PR do lote, mas é redundante.

- **Faz sentido: 4/10** — a intenção é ótima, mas já resolvida.
- **Atende ao padrão: 7/10** — segue convenções e estilo; só está obsoleto.

**Recomendação:** ❌ **Fechar como obsoleto** (não rejeitar pelo mérito — o fix em si é correto e já foi incorporado).

---

## Sugestões de melhoria (processo)

1. **Higiene de branches.** As 4 branches acima e as 5 já mescladas (0 commits à frente) deveriam ser deletadas no remoto para reduzir ruído nos próximos runs.
2. **Validação de convenção no CI.** Adicionar um check que rejeite agents sem `team:` na frontmatter evitaria, na raiz, PRs que regridem esse campo.
3. **Credenciais na automação.** Sem token + `api.github.com` liberado, este relatório não consegue ver PRs reais. Enquanto isso, os parecer continuam sendo sobre o snapshot de 2026-05-07.

---

*Relatório gerado automaticamente. Nenhuma ação de escrita (comentário/merge/close) foi executada no GitHub — apenas leitura local, conforme escopo da tarefa.*
