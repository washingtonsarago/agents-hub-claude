# Verificação de Pull Requests — EMS-NCTECH/agents-hub-claude

**Data da execução:** 2026-06-08 (tarefa agendada `verificao`)
**Autor da revisão:** automação Cowork (modo somente-leitura)

## ⚠️ Limitação de ambiente (ler primeiro)

Esta execução **não conseguiu consultar o estado ao vivo dos PRs no GitHub**:

- `api.github.com` está **bloqueado** no egress da sandbox (`curl` → código `000`).
- Não há **connector de GitHub** disponível no registry, nem `gh` CLI, nem credenciais HTTPS/SSH; `git ls-remote origin` falha (`could not read Username`).
- O repositório é **privado**, então acesso anônimo também não funciona.

Como consequência, a análise é feita sobre os **refs remotos congelados no clone local**. O último commit em qualquer branch candidata é de **2026-05-07**; o último `git fetch` registrado foi **2026-06-02** (sem novidades). As branches analisadas são **candidatas a PR aberto** (branches `origin/*` ainda não mergeadas em `origin/main`) — **podem já ter sido mergeadas, fechadas ou atualizadas no upstream** sem que este snapshot reflita.

**Continuidade:** o estado é **idêntico ao da execução de 2026-06-02** — mesmas 4 branches, mesmos commits (`2fa94b8`, `b5062e5`, `76fa323`, `d047058`), mesmos diffstats. Nenhum PR novo entrou e nenhum dos achados anteriores foi resolvido no snapshot. Os achados abaixo foram **re-verificados** nesta run (não apenas copiados).

**Para tornar a automação confiável:** liberar `api.github.com` em Settings → Capabilities + configurar um token de leitura (ou instalar `gh`). Sem isso, runs diários produzem relatórios espelhados sobre dados estáticos.

---

## Resumo

Quatro branches estão à frente de `origin/main` no snapshot local (candidatas a PR aberto):

| # | Branch | Tipo | Arquivos | Faz sentido | Atende ao padrão | Recomendação |
|---|--------|------|----------|:-----------:|:----------------:|--------------|
| A | `feature/add-cache-search-engineer` | novo agent | `agents/cache-search-engineer.md`, `manifest.json` | 9/10 | 6/10 | Solicitar mudanças |
| B | `feature/add-infra-cost-estimator` | novo agent | `agents/infra-cost-estimator.md`, `manifest.json` | 9/10 | 6/10 | Solicitar mudanças |
| C | `feature/add-technical-writer` | novo agent | `agents/technical-writer.md`, `manifest.json` | 8/10 | 6/10 | Solicitar mudanças |
| D | `fix/regen-no-spurious-date-bump` | fix script | `scripts/regen-manifest.js` | 9/10 | 9/10 | **Aprovar** |

**Achado transversal (A, B, C) — re-verificado nesta run:** os três novos agents **não declaram o campo obrigatório `team:`** na frontmatter (`grep '^team:'` → 0 em cada um). Todos os **18 agents em `main` têm `team:`** (18/18 confirmado). Como o `regen-manifest.js` só emite `team` no manifest quando ele existe na frontmatter, o `--check` **passa mesmo com a violação** — ou seja, isto não é pego por CI e precisa ser barrado na revisão.

---

## A — `feature/add-cache-search-engineer`

**Commit:** `2fa94b8 feat(agents): add cache-search-engineer` · **+334 / −1**

Adiciona um agent de 325 linhas para camadas de cache (Redis/Memcached/ElastiCache) e search (Elasticsearch/OpenSearch). Frontmatter: `model: opus`, `color: red`, `tier: reasoning`. Inserido em ordem alfabética no `manifest.json` (entre `aws-...` e `cypress-...`); `regen-manifest.js --check` passa.

**Pontos fortes**
- Escopo bem delimitado e diferenciado explicitamente de `postgres-dba` e `system-architect` — evita sobreposição de responsabilidades.
- Description rica, com 5 exemplos de uso acionáveis.
- Conteúdo técnico maduro (hit ratio, p99, NDCG, eviction, stampede protection) e Conventional Commit correto.

**Pontos fracos**
- ❌ **Falta `team:` na frontmatter** (obrigatório). Esperado algo como `team: data` ou `team: devops`.

**Sugestões**
- Adicionar `team:` à frontmatter e rodar `node scripts/regen-manifest.js` para repopular a entrada com o bucket de time.

**Parecer:** mudança coerente e de alto valor, tecnicamente sólida, mas viola uma convenção obrigatória do repositório. → **Solicitar mudanças** (correção pequena: 1 linha + regen).

## B — `feature/add-infra-cost-estimator`

**Commit:** `b5062e5 feat(agents): add infra-cost-estimator` · **+271 / −1**

Agent de 262 linhas para estimativa de custo de infra / TCO / FinOps. Frontmatter: `model: opus`, `color: yellow`, `tier: reasoning`. Inserção alfabética correta (entre `go-...` e `integration-architect`); `--check` passa.

**Pontos fortes**
- Diferenciação clara de `aws-devops-engineer` ("modela custo antes de construir").
- Forte ênfase em rastreabilidade (cada número tem unidade, fonte de preço e data) e análise de sensibilidade.
- Conventional Commit correto; manifest íntegro.

**Pontos fracos**
- ❌ **Falta `team:` na frontmatter** (mesma violação de A).

**Sugestões**
- Adicionar `team:` (provável `devops` ou `data`) e rodar o regen.

**Parecer:** agent necessário e bem implementado; mesma pendência de convenção. → **Solicitar mudanças.**

## C — `feature/add-technical-writer`

**Commit:** `76fa323 feat(agents): add technical-writer` · **+247 / −1**

Agent de 238 linhas para documentação voltada ao usuário final. Frontmatter: `model: sonnet`, `color: blue`, `tier: speed`. Anexado ao fim da lista de agents (ordem alfabética correta, após `system-architect`); `--check` passa.

**Pontos fortes**
- Alinhado ao framework Diátaxis (reference/tutorial/how-to/explanation) — base sólida e reconhecida.
- Escopo diferenciado de `project-memory-keeper` e da skill de release-notes.
- Escolha de `sonnet`/`tier: speed` é apropriada para a natureza do trabalho (alto volume, menor exigência de raciocínio profundo).

**Pontos fracos**
- ❌ **Falta `team:` na frontmatter** (mesma violação).

**Sugestões**
- Adicionar `team: docs` (consistente com o bucket de documentação) e rodar o regen.

**Parecer:** boa adição; domínio um pouco mais genérico que A/B mas bem escopado. Mesma pendência. → **Solicitar mudanças.**

## D — `fix/regen-no-spurious-date-bump`

**Commit:** `d047058 fix(regen): only bump updated_at when content actually changed` · **+5 / −6**

Corrige o `scripts/regen-manifest.js` para **só atualizar `updated_at` quando houver mudança real de conteúdo**, evitando churn diário no manifest. A lógica inicializa `next.updated_at` com o valor atual, conta apenas mudanças reais em `totalChanges`, e só seta `todayUTC()` quando `totalChanges > 0` (linhas 231/235/262/267 confirmadas no snapshot). Comentário do cabeçalho atualizado.

**Pontos fortes**
- Resolve diretamente um problema real (commits diários espúrios só por causa da data).
- Diff mínimo e cirúrgico; preserva a detecção de `metadata-only drift`.
- Raciocínio correto inclusive para o fluxo `--check` (que deixa de falhar quando só a data mudaria).
- Conventional Commit correto (`fix(regen):`).

**Pontos fracos**
- Sem teste automatizado cobrindo o novo comportamento — porém consistente com o estilo atual do repo (apenas `--check`).

**Sugestões**
- Opcional: smoke test (rodar regen duas vezes e assertar que `updated_at` não muda na segunda).

**Parecer:** correção limpa, necessária e bem fundamentada, aderente às convenções. → **Aprovar.**

---

## Próximos passos recomendados

1. **Desbloquear a automação:** liberar `api.github.com` no egress + token de leitura (ou connector de GitHub), para que a `verificao` passe a ler PRs ao vivo em vez de refs congelados. **Sem isso, este relatório continuará idêntico a cada run.**
2. **PRs A/B/C:** pedir aos autores o campo `team:` na frontmatter + `node scripts/regen-manifest.js`; após isso são aprováveis.
3. **PR D:** aprovar e mergear.
4. **Reforço de guard-rail (sugestão de projeto):** fazer o `regen-manifest.js --check` (ou um lint de frontmatter) **falhar quando um agent não tiver `team:`**, já que hoje a violação passa silenciosamente pelo CI.
