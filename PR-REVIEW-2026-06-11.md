# Revisão de Pull Requests — agents-hub-claude

**Data:** 2026-06-11 · **Repositório:** `EMS-NCTECH/agents-hub-claude` · **Execução:** tarefa agendada `verificao` (autônoma)

---

## ⚠️ Limitação do ambiente (leia primeiro)

Esta análise **não conseguiu consultar o estado ao vivo dos Pull Requests no GitHub**:

- `api.github.com` continua **bloqueado** pela allowlist de egress (`cowork-egress-blocked`).
- `github.com` agora está na allowlist, mas o repositório é **privado** e a sandbox **não tem credenciais** (`git fetch` falha com `could not read Username for 'https://github.com'`).
- Portanto, a análise se baseia apenas nos **refs remotos do clone local**, congelados no último *fetch* bem-sucedido (`origin/main` = merge do PR #9, 2026-05-07).

**Consequência:** PRs criados *depois* desse fetch são invisíveis aqui. O que segue é a avaliação das únicas branches que aparecem "à frente da main" no clone local.

**Próximo passo para tornar a automação confiável:** liberar `api.github.com` na egress **e** configurar um token (PAT/`gh` CLI) na sandbox. Sem isso, runs diários produzem relatórios espelhados.

---

## Resumo executivo

Nenhuma das 4 branches candidatas representa trabalho novo a ser integrado. **Todas já estão refletidas na `main`** — em três casos com melhoria (o campo obrigatório `team:` foi adicionado durante o merge), e num caso o fix já foi aplicado por outro commit. **Mesclar qualquer uma delas como está causaria regressão.**

| Branch (candidato a PR) | Faz sentido | Atende ao padrão | Recomendação |
|---|:---:|:---:|---|
| `feature/add-cache-search-engineer` | 3/10 | 4/10 | ❌ Rejeitar / fechar (já na main, sem `team:`) |
| `feature/add-infra-cost-estimator` | 3/10 | 4/10 | ❌ Rejeitar / fechar (já na main, sem `team:`) |
| `feature/add-technical-writer` | 3/10 | 4/10 | ❌ Rejeitar / fechar (já na main, sem `team:`) |
| `fix/regen-no-spurious-date-bump` | 4/10 | 7/10 | ❌ Fechar (fix já presente na main) |

---

## 1. `feature/add-cache-search-engineer`

**Commit:** `2fa94b8 feat(agents): add cache-search-engineer` · Washington Sarago · 2026-05-07
**Arquivos:** `agents/cache-search-engineer.md` (+325), `manifest.json`

**Conteúdo.** O agent em si é bem escrito: missão clara, disciplina de memória via `project-memory-keeper`, descrição rica com exemplos de acionamento, distinção explícita de `postgres-dba`/`system-architect`. Frontmatter com `name`, `description`, `model: opus`, `color`, `tier: reasoning`.

**Problemas.**
- **Já está na `main`** como `cache-search-engineer` v**1.0.1** com `team: data`. A branch é uma versão *anterior* desse mesmo agent.
- **Falta o campo obrigatório `team:`** na frontmatter (convenção introduzida por `f33351d`, presente na main). A única diferença entre a branch e a main é justamente que a **main adicionou `team: data`** — ou seja, mesclar a branch *removeria* o campo.
- Faz hardcode de `updated_at: "2026-05-07"` no `manifest.json` em vez de deixar o `regen-manifest.js` gerenciar.

**Notas.** Faz sentido **3/10** (agent bom, mas redundante e regressivo no contexto atual). Atende ao padrão **4/10** (Conventional Commit ✓, posição alfabética no manifest ✓, mas viola a regra obrigatória do `team:`).

**Parecer:** ❌ **Rejeitar / fechar.** Trabalho já incorporado à main com melhoria. Se o PR ainda estiver aberto, fechá-lo referenciando o commit que adicionou o agent com `team: data`.

---

## 2. `feature/add-infra-cost-estimator`

**Commit:** `b5062e5 feat(agents): add infra-cost-estimator` · 2026-05-07
**Arquivos:** `agents/infra-cost-estimator.md` (+262), `manifest.json`

**Conteúdo.** Sólido: modelagem de TCO, análise de sensibilidade, distinção de `aws-devops-engineer` ("modela custo antes de construir"), exemplos concretos. Frontmatter `model: opus`, `tier: reasoning`, `color: yellow`.

**Problemas.** Idênticos ao item 1: **já presente na main** como v**1.0.1** com `team: devops`; a branch **não tem `team:`**; `updated_at` hardcoded. Posição no manifest correta (entre `go-*` e `integration-architect`).

**Notas.** Faz sentido **3/10**. Atende ao padrão **4/10**.

**Parecer:** ❌ **Rejeitar / fechar.** Superado pela main.

---

## 3. `feature/add-technical-writer`

**Commit:** `76fa323 feat(agents): add technical-writer` · 2026-05-07
**Arquivos:** `agents/technical-writer.md` (+238), `manifest.json`

**Conteúdo.** Bem estruturado, alinhado ao framework Diátaxis (reference/tutorial/how-to/explanation), audiência e propósito claros, `model: sonnet`/`tier: speed` (apropriado para tarefa de escrita). Distinção de `project-memory-keeper`.

**Problemas.** Idênticos: **já na main** como v**1.0.1** com `team: docs`; branch **sem `team:`**; `updated_at` hardcoded.

**Notas.** Faz sentido **3/10**. Atende ao padrão **4/10**.

**Parecer:** ❌ **Rejeitar / fechar.** Superado pela main.

---

## 4. `fix/regen-no-spurious-date-bump`

**Commit:** `d047058 fix(regen): only bump updated_at when content actually changed` · 2026-05-06
**Arquivos:** `scripts/regen-manifest.js` (+5 / −6)

**Conteúdo.** Fix correto e bem pensado: inicializa `next.updated_at` com `current.updated_at`, só promove para `todayUTC()` quando há mudança real de conteúdo (`totalChanges > 0`), removendo a contagem artificial da diferença de data. Comentário do cabeçalho atualizado de forma coerente. Mudança pequena e focada, em um único arquivo.

**Problemas.**
- **O fix já está na `main`** — `scripts/regen-manifest.js` na main já contém exatamente essa lógica (`updated_at: current.updated_at` + `next.updated_at = todayUTC()` condicional) e o docstring atualizado. Foi aplicado por outro commit (provável PR #5).
- Sem teste automatizado acompanhando (o script não tem harness; aceitável dado o padrão do repo, mas um teste de fumaça seria desejável).

**Notas.** Faz sentido **4/10** (fix sólido, porém redundante). Atende ao padrão **7/10** (Conventional Commit ✓, diff cirúrgico ✓, sem teste).

**Parecer:** ❌ **Fechar.** Funcionalidade já presente na main; manter a branch só gera ruído.

---

## Recomendação geral

1. **Fechar as 4 branches/PRs** acima — nada a integrar; risco de regressão nos três agents (perda do `team:`).
2. **Desbloquear a automação:** liberar `api.github.com` + token na sandbox para que a `verificao` passe a ler PRs reais em vez do espelho local.
3. Quando a automação tiver acesso real, reavaliar se há PRs novos posteriores a 2026-05-07 (invisíveis nesta execução).

*Relatório gerado automaticamente pela tarefa `verificao` em modo somente-leitura local.*
