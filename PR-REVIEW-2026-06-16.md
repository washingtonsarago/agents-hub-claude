# Revisão de Pull Requests — agents-hub-claude

**Data:** 2026-06-16 · **Repositório:** `EMS-NCTECH/agents-hub-claude` · **Execução:** tarefa agendada `verificao` (autônoma)

---

## ⚠️ Limitação do ambiente (leia primeiro)

Esta análise **não conseguiu consultar o estado ao vivo dos Pull Requests no GitHub**:

- `api.github.com` continua **bloqueado** pela allowlist de egress (`curl` → `HTTP 000`).
- O repositório é **privado** e a sandbox **não tem credenciais** — `git fetch` falha com `could not read Username for 'https://github.com'`. `gh` CLI não está instalado.
- Portanto, a análise se baseia apenas nos **refs do clone local**, congelados no último *fetch* bem-sucedido (`origin/main` = merge do PR #9, **2026-05-07**).

**Consequência:** qualquer PR aberto *depois* desse fetch é invisível aqui. O que segue avalia apenas as branches que ainda aparecem "à frente da `main`" no clone local. Enquanto o ambiente permanecer congelado, este relatório é, por construção, espelhado dos runs anteriores.

**Para tornar a automação confiável:** liberar `api.github.com` na egress **e** prover um token (PAT/`gh` CLI) à sandbox. Sem isso, os runs diários reanalisam dados estáticos.

---

## Resumo executivo

Nenhuma das branches candidatas representa trabalho novo a integrar. **Todas já estão refletidas na `main`**, e em todos os casos a `main` está **à frente** da branch:

- Nos 3 agents, a versão da `main` inclui o campo obrigatório `team:` que a branch **não tem** — mesclar causaria **regressão**.
- No fix de script, a `main` já possui (inclusive ampliou) a lógica de manifest com `team`; a branch é mais antiga e **removeria** esse recurso.

| Branch (candidato a PR) | Commit | Faz sentido | Atende ao padrão | Recomendação |
|---|---|:---:|:---:|---|
| `feature/add-cache-search-engineer` | `2fa94b8` | 3/10 | 4/10 | ❌ Rejeitar / fechar |
| `feature/add-infra-cost-estimator` | `b5062e5` | 3/10 | 4/10 | ❌ Rejeitar / fechar |
| `feature/add-technical-writer` | `76fa323` | 3/10 | 4/10 | ❌ Rejeitar / fechar |
| `fix/regen-no-spurious-date-bump` | `d047058` | 4/10 | 5/10 | ❌ Fechar (stale; regrediria a `main`) |

> Branches a **0 commits à frente** da `main` (já mescladas, nada a revisar): `feature/add-senior-product-designer`, `origin/main`.

---

## 1. `feature/add-cache-search-engineer`
**Commit:** `2fa94b8 feat(agents): add cache-search-engineer` · 2026-05-07
**Arquivos:** `agents/cache-search-engineer.md` (+325), `manifest.json` · base `2026-05-06` (1 commit atrás da `main`)

**Conteúdo.** Agent bem escrito: missão clara, descrição rica com exemplos de acionamento e distinção explícita de `postgres-dba`/`system-architect`. Frontmatter com `name`, `description`, `model: opus`, `color`, `tier: reasoning`.

**Problema decisivo.** O arquivo **já existe na `main`**, e a versão da `main` traz `team: data` — convenção obrigatória do repositório. A branch **não tem** `team:`. Mesclá-la sobrescreveria a versão correta por uma desatualizada.

- **Faz sentido: 3/10** — agent útil, mas o conteúdo já foi integrado; não há trabalho novo.
- **Atende ao padrão: 4/10** — viola a convenção `team:` que a `main` já satisfaz.

**Sugestão de melhoria.** Se houver algo a recuperar da branch, fazê-lo via novo PR rebaseado na `main`, preservando `team: data`.

**Recomendação:** ❌ **Rejeitar / fechar.** Conteúdo já na `main` em estado superior.

---

## 2. `feature/add-infra-cost-estimator`
**Commit:** `b5062e5 feat(agents): add infra-cost-estimator` · 2026-05-07
**Arquivos:** `agents/infra-cost-estimator.md` (+262), `manifest.json` · base `2026-05-06`

**Conteúdo.** Agent coerente para estimativa de custo de infraestrutura; boa frontmatter e descrição.

**Problema decisivo.** Idêntico ao caso anterior: arquivo **já na `main`** com `team: devops`; a branch não traz o campo. Merge = regressão.

- **Faz sentido: 3/10**
- **Atende ao padrão: 4/10**

**Recomendação:** ❌ **Rejeitar / fechar.**

---

## 3. `feature/add-technical-writer`
**Commit:** `76fa323 feat(agents): add technical-writer` · 2026-05-07
**Arquivos:** `agents/technical-writer.md` (+238), `manifest.json` · base `2026-05-06`

**Conteúdo.** Agent de redação técnica bem estruturado.

**Problema decisivo.** **Já na `main`** com `team: docs`; branch sem o campo. Merge = regressão.

- **Faz sentido: 3/10**
- **Atende ao padrão: 4/10**

**Recomendação:** ❌ **Rejeitar / fechar.**

---

## 4. `fix/regen-no-spurious-date-bump`
**Commit:** `d047058 fix(regen): only bump updated_at when content actually changed` · 2026-05-06
**Arquivos:** `scripts/regen-manifest.js` · base `2026-05-05` (a mais antiga das branches)

**Conteúdo.** A intenção do fix — só atualizar `updated_at` quando o conteúdo de fato muda — é legítima e desejável (evita ruído de diff em regenerações do manifest).

**Problema decisivo.** A branch está **defasada**. Comparando com a `main` atual, esta branch **remove** `extractFrontmatterField()` e o bloco que popula `entry.team` no manifest para agents — recurso que a `main` já incorporou (+37/−8 linhas a mais na `main`). Aplicar a branch como está **regrediria** o suporte a `team:` no manifest.

- **Faz sentido: 4/10** — a ideia do fix é boa, mas chega tarde e em conflito com a evolução da `main`.
- **Atende ao padrão: 5/10** — código limpo, porém desatualizado frente ao estado atual.

**Sugestão de melhoria.** Se o comportamento "não bumpar data sem mudança" ainda não estiver 100% na `main`, reextrair apenas essa lógica num PR novo rebaseado, **mantendo** a extração de `team` no manifest.

**Recomendação:** ❌ **Fechar.** Stale; merge causaria regressão no manifest.

---

## Conclusão

Sob os dados disponíveis (refs locais congelados em 2026-05-07), **não há PR aberto que deva ser aprovado**. As 4 branches candidatas já foram superadas pela `main` e mesclá-las introduziria regressões (perda de `team:`). O bloqueio de rede impede confirmar se existem PRs mais recentes — recomenda-se desbloquear `api.github.com` + token para que os próximos runs sejam confiáveis.

*Verificações executadas:* `git diff --stat main...<branch>`, comparação de `team:` (main × branch) em cada agent, e diff de `scripts/regen-manifest.js` (main × branch) confirmando a direção da regressão.
