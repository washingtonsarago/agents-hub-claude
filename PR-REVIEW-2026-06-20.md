# Revisão de Pull Requests — agents-hub-claude

**Data:** 2026-06-20 · **Repositório:** `github.com/washingtonsarago/agents-hub-claude` (privado) · **Execução:** tarefa agendada `verificao` (autônoma)

---

## ⚠️ Limitação do ambiente (leia primeiro)

A consulta **ao vivo** dos Pull Requests no GitHub continua indisponível nesta sandbox:

- `api.github.com` segue **bloqueado** pela allowlist de egress (`curl` → `HTTP 000`).
- A sandbox **não tem credenciais**: `git fetch`/`git ls-remote` manuais falham com `could not read Username for 'https://github.com'`; `gh` CLI não está instalado.
- **Porém**, os refs remotos do clone local **estão atualizados** — houve um *fetch* bem-sucedido por fora da sandbox (`FETCH_HEAD` = **2026-06-20 12:08 UTC**). Logo, as branches `origin/*` refletem o estado real do servidor neste run, ainda que eu não consiga listar diretamente o conjunto "vivo" de PRs abertos via API.

**Para tornar a automação 100% confiável:** liberar `api.github.com` na egress **e** prover um token (PAT/`gh` CLI) à sandbox, permitindo distinguir branch de PR aberto e ler título/descrição/revisões reais.

---

## Resumo executivo

**Nenhum PR novo desde o último run (2026-06-19).** O estado é **idêntico** ao de ontem:

- `origin/main` permanece em `ecd9187 Merge pull request #1` (2026-06-18 18:51) — **sem novos merges**.
- **Nenhuma branch nova** foi criada em `origin`.
- As 3 branches candidatas (`cache-search-engineer`, `infra-cost-estimator`, `technical-writer`) continuam **inalteradas e stale** (commit de 2026-05-07), cada uma **1 commit à frente / 14 atrás** da `main`.

| Branch (candidato a PR) | Commit | Estado | Faz sentido | Atende ao padrão | Recomendação |
|---|---|---|:---:|:---:|---|
| `feature/add-cache-search-engineer` | `2fa94b8` | stale (1↑ / 14↓) | 3/10 | 4/10 | ❌ Rejeitar / fechar (ou rebasear) |
| `feature/add-infra-cost-estimator` | `b5062e5` | stale (1↑ / 14↓) | 3/10 | 4/10 | ❌ Rejeitar / fechar (ou rebasear) |
| `feature/add-technical-writer` | `76fa323` | stale (1↑ / 14↓) | 3/10 | 4/10 | ❌ Rejeitar / fechar (ou rebasear) |

> `feature/add-senior-product-designer` — **0 à frente / 16 atrás**: conteúdo já mesclado na `main`, nada a revisar.
> `feature/ux-mobile-web-agents` — já mesclada via **PR #1**, fechada.
> `fix/regen-no-spurious-date-bump` — branch **apenas local** (não existe em `origin`), não é PR aberto; excluída.

---

## Diagnóstico comum às 3 branches stale

Verifiquei o conteúdo real dos diffs neste run (não é apenas eco dos relatórios anteriores):

**1. Intenção do commit é boa, mas isolada.** O commit próprio de cada branch adiciona **um único agent** bem escrito:
- `cache-search-engineer.md` (+325 linhas) — Redis/Memcached/ElastiCache + Elasticsearch/OpenSearch.
- `infra-cost-estimator.md` (+262 linhas) — estimativa de custo de infraestrutura.
- `technical-writer.md` (+238 linhas) — documentação técnica.

Os três seguem **Conventional Commits** (`feat(agents): ...`) e a estrutura de pastas correta (`agents/` + atualização de `manifest.json`).

**2. Problema bloqueante A — base desatualizada (revert massivo).** Cada branch parte de um `main` antigo (≈2026-05-06). O diff líquido `origin/main..branch` mostra hoje **53 arquivos alterados com ~4.4k–4.5k linhas de deleção** (README, `bin/`, `scripts/`, `docs/`, `test/`). Mesclar como está **reverteria semanas de trabalho já presentes na `main`**.

**3. Problema bloqueante B — falta o campo `team:` (regressão de convenção).** O padrão atual do repositório exige `team:` na frontmatter de cada agent. Confirmado neste run:
- Agent da branch (`cache-search-engineer`): frontmatter tem `model`, `color`, `tier` — **sem `team:`**.
- Agent já mesclado na `main` (`ux-designer-web`): inclui **`team: product`**.

Ou seja, além do revert, mesclar introduziria agents **fora do padrão** vigente.

---

## Notas de avaliação (escala 1–10)

Para os três PRs candidatos, idêntico — mesmo diagnóstico:

- **Faz sentido — 3/10:** o agent em si é coerente e útil, mas o PR como entregável não faz sentido no estado atual (reverteria a `main` e está desatualizado).
- **Atende ao padrão — 4/10:** commit e estrutura corretos, porém falta o campo obrigatório `team:` e a base está defasada.

---

## Parecer final

**Pontos fortes:** agents bem redigidos e relevantes ao catálogo; mensagens de commit em Conventional Commits; estrutura de pastas e atualização de `manifest.json` corretas.

**Pontos fracos:** branches baseadas em `main` antigo (merge = revert de ~4.5k linhas); frontmatter **sem o campo obrigatório `team:`**; nenhuma atualização desde 2026-05-07.

**Sugestões de melhoria (se forem reabertas/aproveitadas):**
1. **Rebasear** cada branch sobre a `main` atual (`ecd9187`) para zerar as deleções espúrias.
2. **Adicionar `team:`** na frontmatter de cada novo agent (ex.: `team: platform`/`team: infra`/`team: product`, conforme a área).
3. Reabrir o PR somente com o diff de **um arquivo** (`agents/<nome>.md` + entrada no `manifest.json`).

**Recomendação:** **Rejeitar/fechar os 3 PRs no estado atual.** O conteúdo dos agents pode ser reaproveitado, mas exige rebase + inclusão de `team:` antes de qualquer merge.

---

### Apêndice — comandos de verificação deste run

```
git log -1 --format='%H %ci %s' origin/main
# ecd9187 2026-06-18 18:51:41 -0300 Merge pull request #1 ...

git rev-list --left-right --count origin/main...origin/<branch>
# cache-search-engineer  14  1   (14 atrás / 1 à frente)
# infra-cost-estimator   14  1
# technical-writer        14  1
# senior-product-designer 16  0  (já mesclada)

git diff --stat origin/main..origin/feature/add-cache-search-engineer | tail -1
# 53 files changed, 231 insertions(+), 4458 deletions(-)
```
